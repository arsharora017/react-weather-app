import WeatherBody from "./WeatherBody";

const Body = () => {
  return (
    <div className="bg-gradient-to-br from-cyan-600 to-blue-500">
      <WeatherBody />
    </div>
  );
};

export default Body;
